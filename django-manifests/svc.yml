apiVersion: v1
kind: Service
metadata:
  labels:
    app: django-app
  name: django-app
spec:
  ports:
  - name: 80-8000
    port: 80
    protocol: TCP
    targetPort: 8000
  selector:
    app: django-app
  type: ClusterIP
status:
  loadBalancer: {}
